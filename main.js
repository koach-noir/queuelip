// main.js
import { invoke } from "@tauri-apps/api/tauri";

// この段階ではシンプルなHello Worldアプリなので、
// 基本的な初期化のみを行います
document.addEventListener('DOMContentLoaded', () => {
  console.log('Queuelip application initialized');
});